type Props = {
  imageSource: string;
  prize: number;
  title: string;
  score: number;
  onClick: () => void;
  isFavorite: boolean;
  switchFavorite: () => void;
};

const ProductCard: React.FC<Props> = ({}: Props) => {
  return <></>;
};

export default ProductCard;
